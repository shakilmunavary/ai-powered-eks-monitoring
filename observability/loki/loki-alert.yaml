groups:
- name: loki-500-errors
  rules:
  - alert: High500Errors
    expr: |
      count_over_time({namespace="default"} |= "500"[1m]) > 2
    for: 2m
    labels:
      severity: critical
      service: sample-app
    annotations:
      description: "500 errors detected in EKS logs"
